# First-boot host-key generation writes /etc/ssh, so the whole directory
# gets a writable overlay. That makes sshd_config and sshd_config.d/
# root-writable at runtime on ssh images as well — a known cost of this
# profile (docs/THREAT_MODEL.md); pointing HostKey= at /var would remove it.
etc/ssh
