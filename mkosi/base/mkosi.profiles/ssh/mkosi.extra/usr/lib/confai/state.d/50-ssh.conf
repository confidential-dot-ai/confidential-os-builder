# First-boot host-key generation requires a writable /etc/ssh. This overlay
# also makes sshd_config and sshd_config.d root-writable at runtime—a
# documented trade-off of the profile. Moving HostKey= under /var would
# remove that requirement. See docs/THREAT_MODEL.md.
etc/ssh
