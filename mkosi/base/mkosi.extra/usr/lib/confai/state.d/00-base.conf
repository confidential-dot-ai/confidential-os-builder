# Directories that get a writable (ephemeral) overlay under the default
# immutable root layout, one path per line, relative to /. Read by the initrd
# after the verity root is mounted; everything not listed here refuses
# writes. Profiles add their own file to this directory rather than editing
# this one. See docs/THREAT_MODEL.md.
var
home
root
tmp
