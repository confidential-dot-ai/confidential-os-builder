# Relative paths that receive ephemeral writable overlays, one per line.
# Unlisted paths remain on the read-only verity root. The initrd fails closed
# if a listed directory is absent; profiles extend the list in separate
# files. See docs/THREAT_MODEL.md.
var
home
root
tmp
